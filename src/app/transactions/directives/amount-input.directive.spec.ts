import { AmountInputDirective } from './amount-input.directive';

describe('AmountInputDirective', () => {
  it('should create an instance', () => {
    const directive = new AmountInputDirective();
    expect(directive).toBeTruthy();
  });
});
