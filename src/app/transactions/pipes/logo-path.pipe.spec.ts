import { LogoPathPipe } from './logoPath-path.pipe';

describe('LogoPathPipe', () => {
  it('create an instance', () => {
    const pipe = new LogoPathPipe();
    expect(pipe).toBeTruthy();
  });
});
