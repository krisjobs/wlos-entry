export enum TransactionType {
    ['Card Patyment'],
    ['Online Transfer'],
    ['Transaction'],
}

export enum TransactionState {
    Paid,
    Confirmed,
    Received
}

export enum SortCriterion {
    Date,
    Beneficiary,
    Amount
}